name: flutter_timetable_view
description: A Demo Application of Flutter Timetable View
version: 0.3.0  # Upgraded version

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  # flutter_timetable_view: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
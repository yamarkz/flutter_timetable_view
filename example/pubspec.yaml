name: flutter_timetable_view_demo
description: A Demo Application of Flutter Timetable View
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
# flutter_timetable_view: ^0.2.0
  flutter_timetable_view:
    path: ../../flutter_timetable_view

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true